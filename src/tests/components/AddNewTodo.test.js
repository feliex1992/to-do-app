import React from 'react';
import { shallow } from 'enzyme';
// import AddNewTodo from '../../components/AddNewTodo';

let addTodo, wrapper;

beforeEach(() => {
  addTodo = jest.fn();
  // wrapper = shallow(<AddNewTodo />)
});

test('should render AddNewToko correctly', () => {
  // expect(wrapper).toMatchSnapshot();
});
